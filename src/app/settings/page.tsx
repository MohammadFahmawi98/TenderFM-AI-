import { ModulePage } from "@/components/module-page";

export default function SettingsPage() {
  return <ModulePage title="Settings" description="Organization, users, RBAC, integrations, storage, AI providers, and audit configuration." emptyTitle="No organization settings loaded" />;
}
